# Database & runtime data
.data/
*.sql
*.db
*.sqlite
*.sqlite3

# Composer binary (not needed in version control)
composer.phar

# PHP vendor dependencies (install via: composer install)
# vendor/   <- keeping vendor tracked since it has PHPMailer deps

# Archives / zips that don't need to be in source control
*.zip
*.tar.gz
*.tar
*.gz
*.rar
*.7z

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Editor / IDE files
.vscode/
.idea/
*.swp
*.swo
*~

# Replit internal files
.cache/
.local/
.agents/
.replit
replit.nix

# Logs
*.log
*.logs

# Uploaded files / user content
files/uploads/
uploads/

# PHP config with sensitive info
config.php
includes/config.php
